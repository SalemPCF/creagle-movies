/* Relative */
import { flatten } from './flatten';
import {
    logInfo, logWarning, logError, logMessage, logSuccess,
} from './log';

export {
    flatten,
    logInfo,
    logWarning,
    logError,
    logMessage,
    logSuccess,
};
