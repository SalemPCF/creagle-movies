/* Relative */
import { flatten } from './flatten';
import {
    logInfo, logWarning, logError, logMessage,
} from './log';

export {
    flatten,
    logInfo,
    logWarning,
    logError,
    logMessage,
};
