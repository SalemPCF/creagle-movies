/**
 * Converts the first character of the given string to a capital letter
 * @param {string} string - The String
 */
export const capitalize = string => string.charAt(0).toUpperCase() + string.slice(1);
